/** Remembers whose page a phone opened, so the home-screen icon lands on it. */
export const ATHLETE_COOKIE = "topset-athlete";
/** The phone's time zone, so "today" on the server is the athlete's today. */
export const TZ_COOKIE = "topset-tz";
/** The coach's last screen, for "open where I left off". */
export const LAST_SCREEN_COOKIE = "topset-last";
